# Troubleshooting: <Issue Title>

## Summary

Briefly describe the issue.

Example:

```text
Git push fails with a 403 permission error because the local machine is authenticated to the wrong GitHub account.
```

## Error Message

Paste the exact error message.

```text
<error-message>
```

## Environment

```text
Operating System: <macOS/Linux/Windows>
Tool: <Git/gcloud/Terraform/Docker/etc.>
Version: <version-if-known>
Repository/Project: <placeholder>
```

## Impact

Describe what the issue prevents.

Example:

```text
Unable to push local commits to the remote GitHub repository.
```

## Root Cause

Explain the most likely cause.

```text
<root-cause>
```

## Diagnostic Commands

Run these commands to confirm the problem.

```bash
<diagnostic-command-1>
<diagnostic-command-2>
<diagnostic-command-3>
```

Expected observations:

```text
<expected-observation>
```

## Resolution Steps

### Step 1: <Step Name>

```bash
<command>
```

### Step 2: <Step Name>

```bash
<command>
```

### Step 3: <Step Name>

```bash
<command>
```

## Validation

Run:

```bash
<validation-command>
```

Expected result:

```text
<expected-result>
```

## If the Issue Persists

Check:

- <Check 1>
- <Check 2>
- <Check 3>

## Prevention

Document how to avoid the issue in the future.

Examples:

- Use SSH aliases for multiple GitHub accounts.
- Verify `git remote -v` before pushing.
- Use least privilege access.
- Keep credentials and identities separated by repository.

## Related Articles

- <Related article 1>
- <Related article 2>
